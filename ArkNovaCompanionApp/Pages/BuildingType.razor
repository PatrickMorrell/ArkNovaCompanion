@if (Model.IsStandard)
{
	<Enclosure Size=@Model.Size Scale=@(Model.IsSelected ? 2 : 1.5) IsProportional=true></Enclosure>
}
else
{
	<FontAwesome IconName="@Model.Icon" Size=@(Model.IsSelected ? "x-3" : "x-4")></FontAwesome>
}

@code {
	[Parameter]
	public BuildingTypeModel Model { get; set; }
}
