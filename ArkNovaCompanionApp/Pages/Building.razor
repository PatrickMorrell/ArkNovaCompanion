@if (Model.IsStandard)
{
	<Enclosure Size=@Model.Size Scale=@(Model.IsOccupied ? 2 : 1.5) IsProportional=true></Enclosure>
}
else
{
	<FontAwesome IconName="@Model.Icon" Size=@(Model.IsOccupied ? "x-3" : "x-4")></FontAwesome>
}

@code {
	[Parameter]
	public BuildingModel Model { get; set; }
}
